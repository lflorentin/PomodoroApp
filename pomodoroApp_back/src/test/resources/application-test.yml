spring:
  datasource:
    url: jdbc:postgresql://db:5432/${POSTGRES_DB_TEST}
    username: ${POSTGRES_USER_TEST}
    password: ${POSTGRES_PASSWORD_TEST}

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: none
